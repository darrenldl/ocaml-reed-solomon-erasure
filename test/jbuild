(jbuild_version 1)

(executable
 ((name matrix_tests)
  (libraries (reed-solomon-erasure
              oUnit
              qcheck))
 ))

(alias
 ((name    runtest)
  (deps    (matrix_tests.exe))
  (action  (run ${<}))))
