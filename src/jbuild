(jbuild_version 1)

(library
 ((name        reed_solomon_erasure)
  (public_name reed-solomon-erasure)
  (modules     reed_solomon_erasure)))

(rule
 ((targets (tables.ml))
  (deps    (../generator/gen_tables.ml))
  (action  (run ${<} -o ${@}))))
